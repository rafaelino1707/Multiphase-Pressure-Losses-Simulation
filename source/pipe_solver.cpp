#include <iostream>
#include <vector>
using namespace std;

void sweep_pipe(double pipe_length, P1) {
    vector[] X = 0::0.01::pipe_length;

    double DeltaP_fric = darcy_weisbach(Re, DeltaX, rho, V)  

}